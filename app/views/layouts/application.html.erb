<!DOCTYPE html>
<html>
  <head>
    <title>Photogram</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header class="sticky-top">
      <nav class="navbar navbar-expand-sm bg-light mb-4">
        <h3 class="navbar-brand">Photogram</h3>
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, class: "#{key}") %>
        <% end %>
        <% if logged_in? %>
          <li class="nav-link"><%= link_to "Profile", user_path(current_user.id) %></li>
          <li class="nav-link"><%= link_to "Feed", feeds_path %></li>
          <li class="nav-link"><%= link_to "Favorites", favorites_path %></li>|
          <li class="nav-link"><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign up", new_user_path %></li>
          <li><%= link_to "Login", new_session_path %></li>
        <% end %>
      </nav>
    </header>
    <div class="container">
        <%= yield %>
      </div>
    </div>
  </body>
  <footer class="bg-dark text-center text-white fixed-bottom">
    <h3>Photogram</h3>
  </footer>
</html>
